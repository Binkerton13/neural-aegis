version: '3.8'

services:
  godot-dev:
    image: barichello/godot-ci:4.2.1
    container_name: neural-aegis-dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    environment:
      - DISPLAY=${DISPLAY}
    network_mode: host
    command: tail -f /dev/null
